### **超詳細設計書：QRコード ファイル転送システム (v2: WebSocket対応)**

#### **1. コンセプトと目標**

*   **コンセプト:** QRコードによるビジュアルなデータ転送と、WebSocketによるリアルタイムなフィードバックを組み合わせた、ブラウザ完結型のハイブリッドファイル転送システム。
*   **目標:** ユーザーが選択したファイルを、一連のQRコードに変換して表示。受信側はそれをカメラで読み取りつつ、受信状況をWebSocketで送信側にフィードバックする。これにより、転送を最適化し、ファイルを高速かつ確実に復元する。
*   **技術スタック:** HTML5, CSS3, JavaScript (ES6+), **Node.js**, **WebSocket (`ws`ライブラリ)**, qrcode-generator, jsQR

---

#### **2. 共通プロトコル仕様 (`common.js`)**

*   **`QR_PAYLOAD_SIZE` (Number):** 1つのQRコードに含まれるペイロードの最大バイト数を`1500`とする。
*   **`QR_TRANSMISSION_INTERVAL` (Number):** QRコードを切り替える基本時間間隔を`500`ミリ秒とする。
*   **`WEBSOCKET_URL` (String):** 接続するWebSocketサーバーのURL。（例: `ws://localhost:8080`）
*   **`QR_CONFIG` オブジェクト:** 誤り訂正レベル`'M'`、バージョン`40`など、QRコード生成に関する設定。
*   **（新規）`METADATA` オブジェクト:** ファイル転送のメタ情報を定義する。
    *   `fileName` (String): 元のファイル名。
    *   `fileSize` (Number): ファイルサイズ。
    *   `fileType` (String): ファイルのMIMEタイプ。
    *   `totalChunks` (Number): 総チャンク数。

---

#### **3. シグナリングサーバー (`server.js`) 機能要件 (新規)**

*   **技術スタック:** Node.js, `ws` (WebSocketライブラリ)
*   **役割:** 送信クライアントと受信クライアントを仲介し、フィードバックメッセージを中継する。
*   **機能:**
    1.  **静的ファイル配信:** `http`モジュールを使い、`index.html`, `receiver.html`, `common.js` などのフロントエンド関連ファイルを配信する。
    2.  **WebSocketサーバー起動:** HTTPサーバーに`ws`サーバーをアタッチする。
    3.  **ルーム管理:**
        *   クライアントを管理するための`rooms`オブジェクトを保持する。
        *   送信側から`create_room`メッセージを受け取ると、ユニークなIDを持つルームを作成し、`room_created`メッセージを返す。
        *   受信側から`join_room`メッセージを受け取ると、指定されたルームに参加させ、ルーム内の送信側に`receiver_joined`メッセージを通知する。
    4.  **メッセージ中継:**
        *   あるクライアントから受信したメッセージ（`feedback`, `complete`など）を、同じルームのもう一方のクライアントにブロードキャストする。
    5.  **接続管理:** クライアント切断時には、ルームから適切に削除する。

---

#### **4. WebSocketメッセージプロトコル (新規)**

クライアントとサーバー間で交換されるJSONメッセージの仕様。

*   **クライアント → サーバー:**
    *   `{ "type": "create_room" }`: 送信側がルーム作成を要求。
    *   `{ "type": "join_room", "payload": { "roomId": "..." } }`: 受信側がルーム参加を要求。
    *   `{ "type": "feedback", "payload": { "roomId": "...", "received_indices": [...] } }`: 受信側が受信済みのチャンクインデックスを通知。
    *   `{ "type": "complete", "payload": { "roomId": "..." } }`: 受信側が全チャンクの受信完了を通知。

*   **サーバー → クライアント:**
    *   `{ "type": "room_created", "payload": { "roomId": "..." } }`: サーバーが送信側に作成したルームIDを通知。
    *   `{ "type": "receiver_joined", "payload": { "roomId": "..." } }`: サーバーが送信側に受信者の参加を通知。
    *   `{ "type": "feedback", "payload": { "received_indices": [...] } }`: サーバーが受信側のフィードバックを送信側に中継。
    *   `{ "type": "complete" }`: サーバーが受信側の完了通知を送信側に中継。

---

#### **5. 送信側 (`transmitter.html`) 機能要件 (改訂)**

**【UIコンポーネントの追加】**
*   `inviteContainer` (`<div>`): 接続用の「招待状QRコード」を表示するエリア。

**【状態管理変数の追加】**
*   `socket` (WebSocket): WebSocketのインスタンス。
*   `roomId` (String): 通信ルームのID。
*   `isReceiverJoined` (Boolean): 受信者が参加したかどうかのフラグ。
*   `receivedIndices` (Set): 受信側から通知された、受信済みチャンクのインデックスを保持するSet。

**【実装ステップ（ロジックフロー）】**
1.  **初期化:**
    *   UI参照取得。`fileInput`に`change`イベントリスナーを追加。
2.  **ファイル選択:**
    *   ファイルが選択されたら、WebSocket接続を開始し、サーバーに`create_room`メッセージを送信する。
3.  **ルーム作成と招待QR表示:**
    *   サーバーから`room_created`メッセージを受信したら、`roomId`を保存する。
    *   `roomId`を含む受信側URL（例: `.../receiver.html?room=xyz123`）を生成し、`qrcode-generator`でQRコード化して`inviteContainer`に表示する。
    *   `statusText`を「受信者にこのQRコードをスキャンさせてください」などに更新。
4.  **受信者の参加待機:**
    *   サーバーから`receiver_joined`メッセージを受信したら、`isReceiverJoined`を`true`に設定。
    *   `inviteContainer`を非表示にし、データ転送の準備（ファイル読み込み、チャンク分割）を開始する。
    *   **メタデータを含んだ最初のQRコード**（ファイル名、総チャンク数など）を生成し、表示を開始する。
5.  **データ転送とフィードバック処理:**
    *   `setInterval`でQRコード表示ループを開始。
    *   **表示ロジック:** ループごとに、`receivedIndices`に含まれて**いない**チャンクの中から、次のチャンクを選択して表示する。これにより、未受信のチャンクが優先される。
    *   **フィードバック受信:** サーバーから`feedback`メッセージを受信するたびに、ペイロードの`received_indices`を自身の`receivedIndices` Setにマージする。
6.  **転送完了:**
    *   サーバーから`complete`メッセージを受信したら、`setInterval`を停止し、`statusText`を「転送完了」に更新する。

---

#### **6. 受信側 (`receiver.html`) 機能要件 (改訂)**

**【状態管理変数の追加】**
*   `socket` (WebSocket): WebSocketのインスタンス。
*   `roomId` (String): 通信ルームのID。

**【実装ステップ（ロジックフロー）】**
1.  **初期化とルーム参加:**
    *   ページ読み込み時、URLのクエリパラメータから`roomId`を取得する。
    *   `roomId`があれば、WebSocket接続を開始し、サーバーに`join_room`メッセージを送信する。
    *   `startScan()`を呼び出してカメラの準備を開始する。
2.  **QRコードスキャン:**
    *   `tick()`ループ内でQRコードのスキャンを試みる（Web Worker利用のアーキテクチャは維持）。
3.  **データ処理とフィードバック送信:**
    *   QRコードをデコードし、チャンクデータ`d`とインデックス`i`を取得する。
    *   **初回受信時:** 最初のQRコードからメタデータ（総チャンク数など）を取得し、`totalChunks`や`receivedChunks`配列を初期化する。
    *   **データ格納:** 重複でなければ`receivedChunks[i]`にデータを格納し、`receivedCount`をインクリメント。UIを更新。
    *   **フィードバック:** チャンクを受信するたび、または数チャンクごとに、現在の`receivedChunks`から受信済みのインデックスリストを作成し、`feedback`メッセージとしてWebSocketでサーバーに送信する。
4.  **受信完了:**
    *   `receivedCount`が`totalChunks`に達したら、`completeReception()`を呼び出す。
    *   **完了通知:** `complete`メッセージをWebSocketでサーバーに送信する。
    *   スキャンを停止し、ファイル再構築処理を行う。
    *   **ファイル再構築:** `Blob`を生成する際、受信したメタデータから`fileName`と`fileType`を使用して、正しい名前と形式でダウンロードできるようにする。

---

#### **7. アーキテクチャの改善（Web Workerと適応型スロットリング）**

**(このセクションは変更なし。受信側のQRコード解析は引き続きWeb Workerで行うのが望ましい)**

---
#### **8. 動作確認の方法 (新規)**

1.  **サーバー起動:**
    *   `npm install ws`でWebSocketライブラリをインストールする。
    *   ターミナルで`node server.js`を実行し、シグナリングサーバーを起動する。
2.  **送信側の操作:**
    *   ブラウザで送信側ページ（例: `http://localhost:8080`）にアクセスする。
    *   転送したいファイルを選択する。画面に「招待状QRコード」が表示される。
3.  **受信側の操作:**
    *   別のブラウザまたはスマートフォンで、送信側の画面に表示された「招待状QRコード」をスキャンする。
    *   受信側ページが開き、自動的にWebSocketルームに参加する。
4.  **転送開始と確認:**
    *   受信者が参加すると、送信側のQRコードが自動的にデータチャンクの表示に切り替わる。
    *   受信側のプログレスバーが進捗し、送信側のコンソールで受信フィードバックが確認できる。
5.  **完了:**
    *   転送が完了すると、受信側にダウンロードリンクが表示され、送信側のQRコード表示が停止する。