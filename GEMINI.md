### **超詳細設計書：QRコード ファイル転送システム (v3: 同期方式)**

#### **1. コンセプトと目標**

*   **コンセプト:** **受信側からのリクエストに基づき、送信側が都度QRコードを生成する**、シンプルで確実な同期的ファイル転送システム。
*   **目標:** ユーザーが選択したファイルをチャンクに分割。受信側が次のチャンクを要求するたびに、送信側が対応するQRコードを1つ表示する。これを繰り返し、ファイルを完全に復元する。
*   **技術スタック:** HTML5, CSS3, JavaScript (ES6+), Node.js, WebSocket (`ws`ライブラリ), qrcode-generator, **qr-scanner**

---

#### **2. 共通プロトコル仕様 (`common.js`)**

*   **`QR_PAYLOAD_SIZE` (Number):** 1つのQRコードに含まれるペイロードの最大バイト数を`700`とする。（読み取りやすいように低めに設定）
*   **`WEBSOCKET_URL` (String):** 接続するWebSocketサーバーのURL。
*   **`QR_CONFIG` オブジェクト:** `typeNumber: 0`（自動選択）、`errorCorrectionLevel: 'M'`など、QRコード生成に関する設定。

---

#### **3. シグナリングサーバー (`server.js`) 機能要件**

*   **役割:** 送信クライアントと受信クライアントを仲介し、**チャンクリクエスト**のメッセージを中継する。
*   **機能:** （v2から大きな変更なし）ルーム管理と、クライアント間のメッセージ中継機能を持つ。

---

#### **4. WebSocketメッセージプロトコル (改訂)**

*   **クライアント → サーバー:**
    *   `{ "type": "create_room" }`: 送信側がルーム作成を要求。
    *   `{ "type": "join_room", ... }`: 受信側がルーム参加を要求。
    *   `{ "type": "request_chunk", "payload": { "roomId": "...", "index": 0 } }`: **(新規)** 受信側が指定したインデックスのチャンクを要求。
    *   `{ "type": "complete", ... }`: 受信側が全チャンクの受信完了を通知。

*   **サーバー → クライアント:**
    *   `{ "type": "room_created", ... }`: サーバーがルームIDを通知。
    *   `{ "type": "receiver_joined", ... }`: サーバーが受信者の参加を通知。
    *   `{ "type": "request_chunk", ... }`: **(新規)** サーバーが受信側のチャンクリクエストを送信側に中継。
    *   `{ "type": "complete" }`: サーバーが受信側の完了通知を送信側に中継。

---

#### **5. 送信側 (`transmitter.html`) 機能要件 (全面改訂)**

**【ロジックフロー】**
1.  **初期化:**
    *   ファイルが選択されたら、`FileReader`でファイルを読み込み、`createChunks()`でチャンク配列をメモリ上に生成しておく。
    *   WebSocketに接続し、`create_room`を要求。
2.  **招待:**
    *   サーバーから`room_created`で`roomId`を受け取り、招待用QRコードを生成・表示する。
3.  **待機:**
    *   サーバーから`receiver_joined`通知を受けたら、`statusText`を「受信者のリクエスト待機中...」に更新し、待機状態に入る。
4.  **QRコード表示:**
    *   サーバーから`request_chunk`メッセージを受信する。
    *   ペイロードの`index`に基づき、メモリ上の`chunks`配列から対応するチャンクデータを取得する。
    *   取得したデータでQRコードを**1つだけ生成**し、`qrcodeContainer`に表示する。
    *   `statusText`を「チャンク ${index} を表示中...」などに更新。
5.  **完了:**
    *   サーバーから`complete`通知を受け取ったら、処理を終了し、画面を「転送完了」表示にする。

---

#### **6. 受信側 (`receiver.html`) 機能要件 (全面改訂)**

**【ライブラリ】**
*   `jsQR`に代わり、**`qr-scanner`**ライブラリを使用する。これにより、カメラアクセス、スキャンループ、Web Workerによるバックグラウンド処理が自動化され、コードが大幅に簡素化される。

**【状態管理変数】**
*   `scanner` (QrScanner): `qr-scanner`のインスタンス。
*   `nextChunkIndex` (Number): 次にリクエストすべきチャンクのインデックス。
*   その他、`receivedChunks`, `totalChunks` などは維持。

**【実装ステップ（ロジックフロー）】**
1.  **初期化:**
    *   URLから`roomId`を取得し、WebSocketに接続してルームに参加する。
    *   `new QrScanner()`でスキャナインスタンスを生成。`<video>`要素と、スキャン成功時のコールバック関数 (`handleScanSuccess`) を渡す。
2.  **最初のチャンクリクエスト:**
    *   WebSocket接続が確立したら、`nextChunkIndex`を`0`に設定し、最初のチャンクを要求する`request_chunk`メッセージをサーバーに送信する。
    *   `scanner.start()`を呼び出して、スキャンを開始する。
3.  **スキャン成功時 (`handleScanSuccess` コールバック):**
    *   **スキャンを一時停止** (`scanner.stop()`)。これにより、同じQRコードを何度も読み取るのを防ぐ。
    *   デコードされたデータ (`result.data`) を`JSON.parse()`でパースする。
    *   **インデックス検証:** パースしたチャンクのインデックス (`chunk.i`) が、リクエストした`nextChunkIndex`と一致することを確認する。
    *   **データ格納:** チャンクを`receivedChunks`に保存し、UI（プログレスバーなど）を更新する。
    *   `nextChunkIndex`をインクリメントする。
4.  **完了判定と次のリクエスト:**
    *   もし全てのチャンクを受信し終えていたら、`completeReception()`を呼び出して完了処理に進む。
    *   まだ受信すべきチャンクが残っている場合は、更新された`nextChunkIndex`を使って、次のチャンクを要求する`request_chunk`メッセージをサーバーに送信する。
    *   リクエスト送信後、**再度スキャンを開始** (`scanner.start()`) し、送信側が表示する次のQRコードを待つ。
5.  **受信完了 (`completeReception`):**
    *   スキャナを完全に停止 (`scanner.destroy()`)。
    *   サーバーに`complete`メッセージを送信。
    *   チャンクからファイルを再構築し、ダウンロードリンクを生成する。

---

#### **7. 動作確認の方法 (改訂)**

1.  **環境構築:** `npm install qr-scanner` を実行する。
2.  **サーバー起動:** `node server.js` を実行。
3.  **フロントエンドのホスティング:** GitHub Pagesにデプロイ済みの`transmitter.html`と`receiver.html`を使用する。
4.  **テストフロー:**
    *   送信側ページを開き、ファイルを選択。招待QRが表示される。
    *   受信側で招待QRをスキャン。ルームに参加する。
    *   受信側が自動的にチャンク0をリクエストする。
    *   送信側がリクエストに応じてチャンク0のQRコードを表示する。
    *   受信側がそれをスキャンし、成功すると自動的にチャンク1をリクエストする。
    *   上記がファイルの終わりまで繰り返されることを確認する。